{
  "name": "search-interface-tests",
  "version": "1.0.0",
  "description": "Testes automatizados da interface de pesquisa",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:debug": "node --inspect-brk node_modules/.bin/jest --runInBand",
    "run-validation": "node run-search-tests.js",
    "start-server": "cd ../../../ && npm run dev",
    "test:ci": "jest --ci --coverage=false --watchAll=false"
  },
  "dependencies": {
    "puppeteer": "^21.5.2",
    "jest": "^29.7.0",
    "jest-html-reporters": "^3.1.7"
  },
  "devDependencies": {}
}